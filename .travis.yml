language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-typescript' ]; then cd .. && eval "mv $currentfolder generator-typescript" && cd generator-typescript; fi

